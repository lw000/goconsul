syntax = "proto3";

package Tpk;

service Greeter {
    rpc Ping(HelloRequest) returns (HelloResponse) {}
}

message HelloRequest {
    string name = 1;
}

message HelloResponse {
    string greeting = 1;
}